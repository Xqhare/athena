[package]
name = "athena"
version = "1.0.0"
edition = "2024"

[dependencies]

[features]
encoding_decoding = ["traits"]
compression = ["encoding_decoding"]
checksum = []
byte_bit = []
traits = []
bit_flags = []

[package.metadata.docs.rs]
all-features = true
